图标文件说明
==========

请在此目录 (src/main/resources) 放置以下文件：

1. icon.png - 程序窗口图标
   - 建议大小：64x64 像素或 128x128 像素
   - 格式：PNG (推荐带透明背景)

图标文件放置后将自动被打包到JAR文件中，程序启动时会自动加载。

如果没有添加图标文件，程序仍然可以正常运行，只是窗口将使用默认Java图标。 